## 目标
房间 2 中把底部常驻的排序面板改为弹窗触发：门旁边新增一个"布告板"热区（外观参考用户附图），点击布告板才弹出排序界面。其余尺寸/底图比例保持不变。

## 改动 `src/components/escape/Room2.tsx`

1. **新增布告板组件（SVG）**
   - 位置：绝对定位在后门旁边（door 在 x≈50%, y≈16%），布告板放在 x≈60%, y≈20% 左右，宽约 8%
   - 外观按附图：顶部红色圆钉 + 两根细绳呈三角形，下方深棕矩形板，板面 4 个奶白色竖向纸条
   - 整体作为 `<button>`，hover 时轻微放大/发光，提示"展品排序"
   
2. **排序界面改为 Modal**
   - 删除当前 `<section className="absolute bottom-3 ...">` 常驻面板
   - 新增 state `sortOpen`，点击布告板时 `setSortOpen(true)`
   - 用现有 `Modal` 组件包裹原排序 UI（卡槽 + 时间轴 + 待排序池 + 错误提示 + 确认/重置按钮）
   - 排序成功后自动关闭 Modal，保留原 `solved` 流程（门可进入下一关 + "进入下一关"按钮）

3. **后门提示文案微调**
   - 未完成排序时点门：提示"先点击门旁的布告板完成展品排序"

4. **保持不变**
   - 底图、10 个展柜热区坐标、D 键调试、Modal 组件、`onComplete` 逻辑、全屏比例
   - 左上角章节标题状态保持当前（已存在，不动）

## 需要确认
- 布告板坐标建议 x=60%, y=20%, w=8%, h=14%（贴在门右侧），可接受吗？还是希望放在门左侧？
