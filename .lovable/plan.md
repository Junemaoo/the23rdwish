## 修复房间 1 线索卡片

1. **交换两个电脑屏幕标题**（`src/components/escape/Room1.tsx` 中 `hotspots` 数组）：
   - `pc-left`（笔记本 / 5:20 伦敦）→ 标题改为 `mjm 的电脑屏幕`
   - `pc-right`（台式机 / 14:20 北京）→ 标题改为 `zcx 的电脑屏幕`
   - 线索本里的标题会自动跟着更新（共用 `title` 字段）。

2. **放大线索详情卡片**：
   - 卡片宽度：`max-w-sm` → `max-w-lg`
   - 卡片内边距：`px-6 py-7` → `px-8 py-9`
   - 图片宽度占比：`w-[75%]` → `w-[85%]`，并加 `max-h-[55vh]` 避免在小屏溢出
   - 标题字号：`text-2xl` → `text-3xl`
   - 按钮：`text-base py-3 w-[70%]` → `text-lg py-3.5 w-[65%]`
   - 元素间距：`gap-5` → `gap-6`

其它不动（飞行动画、收集册、密码锁等）。
